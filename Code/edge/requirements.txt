# Edge unit dependencies (Project Book §5.1).
# Install with: pip install -r requirements.txt
#
# These are the lightweight deps needed to run the edge unit, including
# --simulate mode. They install cleanly on Python 3.10-3.12.
opencv-python==4.9.0.80
paho-mqtt==1.6.1
numpy>=1.24,<2.0
python-dotenv==1.0.1

# Computer-vision models (YOLOv8n + torch) are heavy and version-sensitive,
# and are NOT needed for --simulate. To run real webcam detection install them
# separately:
#     pip install -r requirements-cv.txt
